name: flan_flutter
description: Flutter extensions enabling AI agents to inspect and interact with running Flutter apps via Flan MCP.
version: 0.5.0
resolution: workspace
homepage: https://github.com/keyvez/flan-mcp
repository: https://github.com/keyvez/flan-mcp
issue_tracker: https://github.com/keyvez/flan-mcp/issues
topics:
  - flutter
  - mcp
  - testing
  - ai
  - automation

environment:
  sdk: "^3.6.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  logging: ^1.3.0
  shared_preferences: ^2.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
