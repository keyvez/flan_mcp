name: flan_mcp
description: MCP server for Flutter app interaction - enables AI agents to inspect and interact with running Flutter apps via VM service.
version: 0.5.0
resolution: workspace
homepage: https://github.com/keyvez/flan-mcp
repository: https://github.com/keyvez/flan-mcp
issue_tracker: https://github.com/keyvez/flan-mcp/issues
topics:
  - flutter
  - mcp
  - testing
  - ai
  - automation

executables:
  flan_mcp: flan_mcp

environment:
  sdk: ^3.10.0

dependencies:
  args: ^2.7.0
  logging: ^1.3.0
  mcp_dart: ^1.2.1
  vm_service: ^15.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.2
